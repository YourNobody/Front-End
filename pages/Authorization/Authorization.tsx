import React from 'react';
import { AuthorizationProps } from './Authorization.props';
import styles from './Authorization.module.css';

export const Authorization = ({ children }: AuthorizationProps): JSX.Element => {
  return (
    <></>
  );
};