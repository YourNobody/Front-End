import React from 'react';
import { ProfileProps } from './Profile.props';
import styles from './Profile.module.css';

export const Profile = ({ children }: ProfileProps): JSX.Element => {
  return (
    <></>
  );
};